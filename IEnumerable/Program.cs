using System.Collections;
using Microsoft.Win32.SafeHandles;

class Program
{
    public static void Main(string[] args){
        // Student s1 = new Student(){Name = "Abhishek", ID = 1};
        // Student s2 = new Student(){Name = "Abhishek", ID = 1};

        // Console.WriteLine(s1.Equals(s2));

        // Product product1 = new Product(){Name = "SmartPhone", ID = 2, Price = 45};
        // Product product2 = new Product(){Name = "SmartPhone", ID = 1, Price = 45};

        // bool result = product1.Equals(product2);
        // Console.WriteLine($"Is product same : {result}");

        // Batch b = new Batch();
        // IEnumerator ER = b.GetEnumerator();
        // ER.MoveNext();
        // Student s = ER.Current as Student;
        // Console.WriteLine(s);
 
        // while(ER.MoveNext()){
        //    Console.WriteLine(ER.Current as Student);
        // }

        // Department d = new Department();
        // IEnumerator ER = d.GetEnumerator();
        // ER.MoveNext();
        // Employee emp = ER.Current as Employee;
        // Console.WriteLine(emp);
 
        // while(ER.MoveNext()){
        //    Console.WriteLine(ER.Current as Employee);
        // }

        // string str = "I am Abhishek Agnihhotr? .";
        // Console.WriteLine(str.WordCount());
        // Console.WriteLine(str.WordCount());

        int num = 2;
        Console.WriteLine(num.MulBy2());

        // string str = "ABCDEF";
        // Console.WriteLine(str.customReverse());
        

    }
}